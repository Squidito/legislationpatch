# Dev Setup

## Serving Locally

```bash
npx serve . --listen 3131
```
Then open `http://localhost:3131`. Playwright MCP requires http:// (no file://).


Env vars in `.env`: `CONGRESS_API_KEY`, `GOVINFO_API_KEY`, `CONGRESS_SESSION=119`.
